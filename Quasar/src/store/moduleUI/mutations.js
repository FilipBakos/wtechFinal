export const updateLeftDrawerState = (state, opened) => {
  state.layout.leftDrawerState = opened
}

export const updateRightDrawerState = (state, opened) => {
  state.layout.rightDrawerState = opened
}
